import { StatusHighlightDirective } from './status-highlight.directive';

describe('StatusHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
